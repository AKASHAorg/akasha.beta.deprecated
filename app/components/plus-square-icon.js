import PropTypes from 'prop-types';
import React from 'react';
import { Icon } from './';

const PlusSquareIcon = () => (
  <div className="flex-center content-link plus-square-icon">
    <Icon type="plus" />
  </div>
);

PlusSquareIcon.propTypes = {

};

export default PlusSquareIcon;
